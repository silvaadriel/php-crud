version: "2.2"

services:
  nginx:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - "./src:/src"
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
  php:
    image: php:7.2-fpm
    volumes:
      - "./src:/src"
